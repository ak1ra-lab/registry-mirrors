gcr-registry.REPLACE_ME_DOMAIN {
	reverse_proxy gcr-registry:5000
	import dns_cloudflare
}

k8s-registry.REPLACE_ME_DOMAIN {
	reverse_proxy k8s-registry:5000
	import dns_cloudflare
}

docker-registry.REPLACE_ME_DOMAIN {
	reverse_proxy docker-registry:5000
	import dns_cloudflare
}

quay-registry.REPLACE_ME_DOMAIN {
	reverse_proxy quay-registry:5000
	import dns_cloudflare
}
